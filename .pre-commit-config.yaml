repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.2
    hooks:
      # Lint and auto-fix
      - id: ruff
        args: [--fix]
      # Format (only files being modified — never bulk-format)
      - id: ruff-format
